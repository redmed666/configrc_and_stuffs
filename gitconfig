[user]
	name = med
	email = khemir.med@gmail.com
[core]
        editor = vim
[diff]
        tool = vimdiff
[difftool]
        prompt = false
[push]
        default = tracking
[alias]
        st = status
        co = checkout
        lg = log --graph --pretty=format:'%C(cyan)%h%C(reset) -%C(yellow)%d%Creset %s %Cgreen(%cd) %C(bold blue)<%an>%Creset' --abbrev-commit --date=local
        br = branch
        diffstat = diff --stat=150,150
[color]
        branch = auto
        diff = auto
        interactive = auto
        status = auto
[color "branch"]
        current = green bold
        local = green
        remote = red bold
[color "status"]
        added = green bold
        changed = yellow bold
        untracked = red
